# Database connection details
DB_HOST=localhost         # or your Postgres container/service name
DB_PORT=5432
DB_NAME=gitlogs
DB_USER=gituser
DB_PASSWORD=gitpass

# Optional Git configuration (if needed)
GIT_AUTHOR_NAME=CI Bot
GIT_AUTHOR_EMAIL=ci-bot@example.com
